Meta-repository containing my development environment.

# Setup commands:
git clone http://github.com/deronnek/dotenv.git
#
mv .bashrc .bashrc_orig
mv .bash_aliases .bash_aliases_orig
mv .screenrc .screenrc_orig
mv .vimrc .vimrc_orig
mv .vim .vim_orig
#
ln -s dotenv/dotbash/.bashrc .bashrc
ln -s dotenv/dotbash/.bash_aliases .bash_aliases
ln -s dotenv/dotbash/.screenrc .screenrc
ln -s dotenv/.vim/vimrc .vimrc
ln -s dotenv/.vim .vim
cd dotenv
git submodule init
git submodule update
cd .vim
git submodule init
git submodule update
cd ~/

# Note: submodule updates may not work if you haven't committed/pushed the latest versions.
